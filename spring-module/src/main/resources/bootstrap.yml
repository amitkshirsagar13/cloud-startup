spring:
  application:
    name: spring-module
  profiles:
    active: local
  cloud:
    config:
      label: master
      uri: http://${VHOSTNAME:${HOSTNAME:localhost.shinigami.io}}:${VPORT:8761}/registry/config
      username: config
      password: config